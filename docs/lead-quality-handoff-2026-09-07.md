# Точка продолжения исправлений карточек

Работа НЕ завершена и НЕ готова к деплою. Существовавшие ранее 81 тест и проверка типов проходили ДО перечисленных ниже новых изменений. Новые изменения ещё не проверены.

## Уже записано

- Общая очистка текста, построение заголовков, начальная корректировка антиспама и кнопка Google Maps.
- В prisma/schema.prisma добавлены contentFingerprint и duplicateOfId с отношением LeadDuplicates.
- Добавлена миграция prisma/migrations/20260907010000_lead_content_identity/migration.sql.
- src/lib/lead-identity.ts: SHA-256 полного очищенного текста и телефона, ошибка DuplicateLeadError.
- src/lib/lead-identity-backfill.ts и scripts/backfill-lead-content.mjs: возобновляемая индексация старых лидов с отметкой копий без удаления записей и покупок.
- В package.json добавлен leads:backfill и его выполнение после миграций перед запуском приложения.
- createLeadWithDeliveries нормализует полный исходный текст и защищён уникальным индексом; лид и задания доставки создаются прежней транзакцией.
- Ingest обрабатывает DuplicateLeadError; max-parser передаёт original в сохранение и запоминает сообщение при обнаружении копии.

## Обязательно закончить

1. Проверить фактический git diff: операции сохранения могли завершаться частично.
2. В max-parser прежнюю предварительную проверку rawText: stableText заменить на rawText: cleanLeadText(original), иначе сравнение обрезанных 1500 символов может скрывать разные объявления.
3. В API пользовательских лидов подключить cleanLeadText перед выводом и редактированием контактов. Для общей ленты фильтровать duplicateOfId: null; покупки и прямой доступ по ID сохранить.
4. В API администратора подключить cleanLeadText и buildLeadTitle, исключить отмеченные копии из общего списка.
5. Применить одинаковое отображение ссылок к административному просмотру: сейчас selectedLead.rawText выводится обычным текстом. Карточка пользователя уже использует leadMapLabel.
6. Уточнить hasVacancyIntent: одного слова «вакансии» недостаточно для отмены антиспама. Сохранить стоп-фразы администратора, не считать профессию «маркетолог» спамом.
7. Проверить очистку по скриншотам и на зарплате/адресе/телефоне/расписании отдельными строками. Не удалять значимые числа.
8. Добавить поведенческие тесты: разные чаты и режимы, изменившиеся счётчики, различия после 1500 символов, сохранение разных контактов/адресов/оплаты, возобновление индексации старых записей, заголовки продавца-кассира и двух курьеров, Google Maps и поддельные домены.
9. Выполнить prisma generate, npm test, npm run typecheck, npm run build. Убедиться, что scripts/parser_worker.py, аккаунты, прокси и регистрация не изменены.
10. Проверить миграцию/индексацию на изолированной тестовой БД при её наличии. Производственную БД в этой работе не трогали, deploy/push не выполнялись.

## Сбой среды

7 сентября 2026 команды и apply_patch работают с перебоями. Последние ошибки: тайм-аут runner pipe-in; CreateProcessAsUserW failed: 5; падение setup helper с 0xc0000005 / -1073741819. Причина не установлена. Новые изменения прав Windows не выполнялись. При устойчивом отказе перезапустить Codex и продолжить в исходной папке по этому документу.

Не изменять scripts/parser_worker.py и механизм получения сообщений. Не удалять пользовательские check_errors.ts и test_db.ts. Не считать прежние проверки подтверждением новых изменений.
